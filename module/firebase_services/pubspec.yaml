name: firebase_services
description: A new Flutter package for firebase services.
version: 0.0.1
homepage:

environment:
  sdk: '>=2.18.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # firebase packages
  cloud_firestore: ^3.4.7
  firebase_auth: ^3.8.0
  firebase_core: ^1.22.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # development
  firebase_auth_mocks: ^0.8.5+1
  very_good_analysis: ^3.1.0

flutter:
